# postrack — 프로그램 핵심 내용 정리

> 이 문서는 폴더 전체(00~03 + 루트 스크립트)를 훑어 만든 요약이다. 세부 근거는 각 하위 문서를 참조.

## 1. 목적 (한 줄)

**C-arm이 수술대 기준으로 어디에 있는지**를 공유 가능한 위치 표현으로 제공한다.
현재 확보된 것은 그 사슬의 한 항 — **열화상 영상에서 회전 없이 발생하는 테이블 평면상 카메라 X·Y 이동**을 추정하는 것.
교수님 요청 건이며, ①카메라 모듈 선정 ②XY 이동 추정 실험 두 갈래가 같이 진행된다.

- 추정 대상: 누적 위치 `(X_mm, Y_mm)`, 프레임 간 이동량 `(dX_mm, dY_mm)`
- 기준 평면: 임상용 X-ray 테이블 상면 `1960 mm × 586 mm` (X=장축, Y=단축)
- 전제: 카메라 높이·방향 고정, 테이블 평면과 평행하게 XY로만 이동
- **범위 제외**: 환자 위치·자세, 조사영역 최적화, 선량 관리, 3축 자세(roll/pitch/yaw), 해부학적 조사 위치 재현
- 장비 기준점 선정·수술대 원점 초기화·카메라-장비 관계 정의는 아직 미정 (다음 단계 과제)

## 2. 폴더 구조

| 폴더 | 내용 |
|---|---|
| `00_프로젝트_개요/` | 연구 목표, 좌표계 규약 정의 |
| `01_입력자료/` | 카메라 사양 비교표, 열화상 이미지/영상, 참고 이미지 |
| `02_실험/` | 실험 영상, 처리 코드(`code/`), 설정(`config/`), 센서 로그(`test2/`) |
| `03_결과/` | 실험별 산출물 (KLT 사전확인, XY추정 v1, test2 IR-only, 라이브 XY v1) |
| 루트 | `slides_fill.py`(발표자료 자동생성), `test_oak_stream.py`(OAK 카메라 스트림 테스트), `_STATUS.md`(진행상황 트래커), 발표 pptx 2종 |

## 3. 방법론 (핵심 파이프라인)

정적 기준점(체커보드) 검출 → KLT로 추적 → RANSAC/median으로 이상점 제거 → 공통 픽셀 이동량 `(du,dv)` → mm 변환 → 누적.

`02_실험/code/` 스크립트 흐름 (XY추정 v1 기준):

1. `bootstrap.py` — 프레임간 강체 변환 누적 (마스크 전파·격자 인덱스 예측용, 최종 REF에는 미사용)
2. `calibrate.py` — 체커보드의 이상 정수격자 제약으로 렌즈 왜곡 자가보정 → **개선 0.1%로 무시 가능**
3. `ref_track.py` — **REF(기준 궤적)**: 격자좌표→영상 호모그래피를 **매 프레임 독립 적합**해 누적 드리프트를 원천 제거 (프레임간 체이닝 방식은 왕복 후 3~4mm 드리프트가 남아 폐기)
4. `validate_ref.py` — REF 자체 신뢰도 검증 V1~V6
5. `estimate_xy.py` — **피검 추정기**: 중앙 열화상 인쇄물(PRINT) 영역만 마스킹 → KLT → median/MAD 이상점 제거 → 공통 변위 → mm 변환 → 누적 (`fixed`=t=0 축척 고정 / `oracle`=매 프레임 REF 축척 사용, 비교 진단용)
6. `degrade.py` — 해상도×노이즈 열화 스윕 (모듈 선정 근거용)
7. `report.py` / `compose_xy_v1_dashboard.py` / `export_xy_v1_rerun.py` — CSV·그래프·오버레이 영상·Rerun 기록 생성
8. `live_xy.py` — 라이브 OAK 스트림(또는 녹화 영상)에서 클릭 캘리브레이션 후 실시간 XY 오버레이하는 GUI 도구 (별도 트랙, `03_결과/라이브_XY_v1/`)
9. `run_test2_ir_only.py` / `export_test2_rerun.py` — test2 데이터셋(IMU 동시 촬영)용 별도 파이프라인

REF와 EST는 사용 정보를 완전히 분리한다: REF는 체커보드 격자만, EST는 체커보드·자·테이블 등을 마스킹하고 중앙 열화상 인쇄물 질감만 사용.

## 4. 결과 요약

### 4.1 XY추정 v1 (`03_결과/XY추정_v1/`) — 핵심 실험, 정본

입력: 커팅매트 위 열화상 인쇄물 + 10mm 체커보드 3장, 폰 카메라 하향 이동 촬영 (1920×1080, 1206프레임, 20.3초, 총 이동 773mm)

- **REF 자체 검증**: 무이동 드리프트 0.038mm, 200mm 왕복 후 잔차 0.228mm, 좌·상 보드 교차검증 0.112mm → 관문 통과. 단 격자 주기성으로 인한 1% 수준 최대 5.5mm 단발 글리치 존재(V5 FAIL, 미해결 한계)
- **추정기 결과 (fixed, 주 결과)**: 최종 오차 **6.36mm (0.82%)**, 정지 7.6초 드리프트 **0.017mm**, 추적 실패 0프레임
- **오차 분해**: 순수 추적 오차(oracle) 3.43mm(0.44%), 축척·방위 기여 9.75mm — 원인은 **손에 든 카메라의 롤(+2.5°)·높이 변동(±3~4%)**이며, 실물 고정 마운트에는 없는 조건
- **대조 실험**: 같은 추정기를 체커보드 영역에 돌리면 오히려 8.13mm(1.05%)로 더 나쁨 → 성능 한계는 열화상 텍스처가 아니라 **프레임 간 누적 자체**
- **해상도×노이즈 스윕**: 160×120급까지도 누적 오차 1.05% 이내, 추적 실패 0회 → **XY 이동 추정만 놓고 보면 해상도는 병목이 아님**. 단 인쇄물 목업이라 실제 센서 NETD/FPN 특성은 미검증이므로 이 결과만으로 저해상도 모듈을 확정하면 안 됨

### 4.2 test2 IR-only (`03_결과/test2_IR_only/`)

휴대폰 IMU와 동시 촬영한 별도 영상(484프레임, 30fps)에서 중앙 IR 인쇄물 텍스처만으로 추정.

- 최종 오차 3.55mm, RMS 1.40mm, p95 3.92mm
- LEFT/TOP 보드 REF 교차검증 median 0.147mm
- IMU는 추정에 사용하지 않고 보조 비교 곡선으로만 표시

### 4.3 라이브 XY v1 (`03_결과/라이브_XY_v1/`)

`live_xy.py` GUI 도구 — OAK 카메라(또는 녹화 영상)에서 실시간 클릭 캘리브레이션 후 XY 오버레이. **전제: 피사체는 움직이지 않는다** (화면 전체 이동=카메라 이동으로 간주, 아직 "피사체 움직임 분리"는 이 도구 범위 밖).

- 리팩터링(`common.py`) 후 기존 결과와 0.0006mm 이내로 일치 확인(회귀 검증 통과)
- 녹화 영상 재생 시 오차 9.7mm(1.25%), 기존 LEFT 대조실험과 동급
- 실제 OAK-D-PRO-POE 장치 스트림 수신·재연결 확인
- 확인된 위험: FOV-보드 크기 충돌(실제 테이블에서 10mm 칸이 너무 작아질 수 있음), 체커보드 검출에 CLAHE 사용 금지(RMS 16.7px→0.30px), 컬러 카메라는 롤링셔터라 부적합, 무한 세션 시 개루프 드리프트 누적, OAK 강제종료 시 ~30초 잠금

## 5. 아직 빠진 것 / 다음 단계

1. **피사체 움직임 분리 미검증** — 지금까지 실험은 전부 정지한 인쇄물 대상. 원래 블로커("카메라 이동 vs 사람 움직임 분리")의 절반만 풀림. **다음 촬영의 핵심 목적**
2. **실물 열화상 센서 아님** — 인쇄된 열화상 사진을 RGB 폰 카메라로 촬영한 목업
3. **목업 축척** — FOV 384mm 대 실제 테이블 1960mm
4. 장비 기준점 선정, 수술대 원점 초기화 절차, 카메라-장비 기준점 관계 정의 모두 미정
5. 카메라 모듈 최종 선정 전 (`01_입력자료/카메라_사양/` 비교표는 있으나 미확정)

다음 촬영 조건(정리): 움직이는 대상 포함, 고정 마운트(롤·높이 고정), 노출/화이트밸런스/초점 고정 + 빠른 셔터, 대상 장축 양쪽에 보드 배치, 프레임간 이동 5mm 이내 유지, 가능하면 실제 열화상 모듈로 동시 촬영.

## 6. 참고 문서 지도

- 진행 상태·다음 액션: `_STATUS.md`
- 목표/좌표계 정의: `00_프로젝트_개요/README.md`, `00_프로젝트_개요/좌표계_정의_및_공유가능_위치표현_2026-09-09.md`
- 실험 입력·판정 기준: `02_실험/README.md`, `02_실험/test2/README.md`
- 결과 해석 상세: `03_결과/XY추정_v1/report.md` (가장 상세), `03_결과/XY추정_v1/FINAL_REVIEW.md` (수식·다이어그램 포함 정리본)
- 라이브 도구 사용법: `03_결과/라이브_XY_v1/README.md`
- 핵심 원리 수식 정리와 파이프라인 구조도: `00_프로젝트_개요/핵심_기능_및_작동원리_수식_2026-09-18.md`, `00_프로젝트_개요/pipeline_map.pdf`
